node('ppcle') {

    JDK_FOLDER = "jdk"
	JAVA_BIN = "${WORKSPACE}/${JDK_FOLDER}/bin"
    SPEC = 'linux_ppc-64_cmprssptrs_le'
    JAVA_VERSION = 'SE90'
    TEST_TARGET = 'extended'

    checkout scm
    def buildfile = load "${WORKSPACE}/buildenv/jenkins/test"
    buildfile.test()
}
