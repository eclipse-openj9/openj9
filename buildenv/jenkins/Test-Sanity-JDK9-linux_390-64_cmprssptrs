node('390') {

    JDK_FOLDER = "jdk"
	JAVA_BIN = "${WORKSPACE}/${JDK_FOLDER}/bin"
    SPEC = 'linux_390-64_cmprssptrs'
    JAVA_VERSION = 'SE90'
    TEST_TARGET = 'sanity'

    checkout scm
    def buildfile = load "${WORKSPACE}/buildenv/jenkins/test"
    buildfile.test()
}
